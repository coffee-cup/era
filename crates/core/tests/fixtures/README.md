# Core fixtures

These fixtures lock canonical object encodings that are part of Era's storage contract.

Use fixtures here for small golden-format compatibility checks only. Prefer temp-directory setup for behavior tests that exercise repositories, object stores, or materialization flows.
